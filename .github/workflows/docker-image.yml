name: Docker Image CI

on:
  push:
    branches: [ "main" ] 

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
        docker build -t app .
        docker login -u vivekshaurya -p dckr_pat_TIvFqMK-P29qN81f4Qo0ujTb6iE
        docker tag vivekshaurya/form
        docker push vivekshaurya/form
        
